// Online C compiler to run C program online
#include <stdio.h>

int main() {
int a[]={1,4,3,5,7};
int n;
int temp=a[0];
n=sizeof(a)/sizeof(a[0]);
for(int i=0;i<n;i++){
    a[i]=a[i+1];
    a[n]=temp;
}
for(int i=0;i<n;i++){
    printf("%d",a[i]);
}
}
